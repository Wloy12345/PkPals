@model PK_EF.CATEGORY

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/SupplyCategoryItems/Views/Shared/_Menu.cshtml";
}

<h2>Details</h2>

<div>
    <h4>CATEGORY</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            Supplies in Categories
        </dt>

        @foreach (var m in Model.SUPPLIES)
        {
            <dd>
                @Html.ActionLink(m.Name, "Details", "SupplyHome", new { area = "Supplies", id = m.SuppliesID }, null)
            </dd>
        }
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.CategoryID }) |
    @Html.ActionLink("Delete", "Delete", new { id = Model.CategoryID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
